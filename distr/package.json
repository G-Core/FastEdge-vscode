{
	"name": "fastedge",
	"displayName": "FastEdge app player",
	"version": "0.1.0",
	"publisher": "Gcore",
	"description": "Player for FastEdge apps",
	"repository": "https://github.com/G-Core/FastEdge-vscode",
	"author": {
		"name": "qrdl",
		"email": "ilya@qrdl.com"
	},
	"engines": {
		"vscode": "^1.61.0"
	},
	"categories": [ "Debuggers" ],
	"activationEvents": [ "onDebug" ],
	"main": "./extension.js",
	"contributes": {
		"debuggers": [
			{
				"type": "fastedge",
				"label": "FastEdge App Runner",
				"program": "./runner",
				"languages": ["rust"],
				"configurationAttributes": {
					"launch": {
						"properties": {
							"binary": {
								"type": "string",
								"description": "Wasm binary path, typically populated automatically"
							},
							"port": {
								"type": "number",
								"description": "Port to listen for incoming requests",
								"default": 8181
							},
							"geoIpHeaders": {
								"type": "boolean",
								"description": "Add sample GeoIP headers",
								"default": false
							},
							"headers": {
								"type": "object",
								"description": "Extra request headers"
							},
							"env": {
								"type": "object",
								"description": "Environment variables"
							},
							"mem_limit": {
								"type": "number",
								"description": "Memory limit in bytes",
								"default": 10000000
							}
						}
					}
				},
				"initialConfigurations": [
					{
						"name": "FastEdge App Runner: Launch",
						"type": "fastedge",
						"request": "launch",
						"env": {
							"var": "value"
						}
					}
				],
				"configurationSnippets": [
					{
						"label": "FastEdge App Runner: Launch",
						"description": "Execute FastEdge app locally.",
						"body": {
							"name": "FastEdge App",
							"type": "fastedge",
							"request": "launch"
						}
					}
				]
			}
		]
	}
}
